<Window x:Class="YoableWPF.ModelManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="{DynamicResource ModelManager_Title}"
        Height="500"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="400"
        MinWidth="500">

    <Window.Resources>
        <!-- Accent color for buttons (uses system accent) -->

        <!-- Modern Button Style -->

        <!-- Secondary Button Style -->

        <!-- Danger Button Style -->

        <!-- Model Card Style -->
        <Style x:Key="ModelCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SystemControlPageBackgroundChromeLowBrush}"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" 
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                Padding="24,20">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock FontFamily="Segoe MDL2 Assets" 
                               Text="&#xE950;" 
                               FontSize="24"
                               Foreground="{StaticResource GlyphPurpleBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"/>
                    <TextBlock Text="{DynamicResource ModelManager_Title}" 
                               FontSize="20" 
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock x:Name="InfoText"
                           Text="{DynamicResource ModelManager_NoModelsLoaded}"
                           Foreground="Gray"
                           TextWrapping="Wrap"
                           FontSize="13"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Padding="24,20">
            <StackPanel>
                <!-- Model List Card -->
                <Border Style="{StaticResource ModelCard}" Padding="0" Margin="0,0,0,16">
                    <ListBox x:Name="ModelListBox"
                             SelectionMode="Single"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             MinHeight="200">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Border" 
                                                    Background="Transparent"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}"
                                                    Margin="2">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource SystemControlHighlightListLowBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentButtonBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               FontFamily="Segoe MDL2 Assets"
                                               Text="&#xE82D;"
                                               FontSize="16"
                                               Foreground="{StaticResource GlyphPurpleBrush}"
                                               VerticalAlignment="Center"
                                               Margin="0,0,12,0"/>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DisplayName}" 
                                               FontSize="13"
                                               VerticalAlignment="Center"/>

                                    <Border Grid.Column="2"
                                            Background="{StaticResource InfoSubtleBrush}"
                                            CornerRadius="10"
                                            Padding="8,4">
                                        <TextBlock Text="{Binding ModelType}"
                                                   Foreground="{StaticResource InfoBrush}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <!-- Warning Card -->
                <Border Background="{StaticResource StatusReviewSubtleBrush}"
                        CornerRadius="6"
                        Padding="16">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text="&#xE7BA;"
                                   FontSize="16"
                                   Foreground="{StaticResource StatusReviewBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,10,0"/>
                        <TextBlock Text="{DynamicResource ModelManager_WarningMessage}"
                                   Foreground="{StaticResource StatusReviewDeepBrush}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer with Buttons -->
        <Border Grid.Row="2"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                Padding="24,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AddButton" 
                        Content="{DynamicResource ModelManager_AddModel}" 
                        Click="AddButton_Click"
                        Style="{StaticResource ModernButton}"
                        MinWidth="100"
                        Margin="0,0,8,0"/>
                <Button x:Name="EditButton" 
                        Content="{DynamicResource ModelManager_EditMapping}" 
                        Click="EditButton_Click"
                        Style="{StaticResource ModernButton}"
                        MinWidth="110"
                        Margin="0,0,8,0"
                        IsEnabled="False"/>
                <Button x:Name="RemoveButton" 
                        Content="{DynamicResource ModelManager_Remove}" 
                        Click="RemoveButton_Click"
                        Style="{StaticResource DangerButton}"
                        MinWidth="90"
                        Margin="0,0,8,0"
                        IsEnabled="False"/>
                <Button x:Name="ClearButton" 
                        Content="{DynamicResource ModelManager_ClearAll}" 
                        Click="ClearButton_Click"
                        Style="{StaticResource DangerButton}"
                        MinWidth="90"
                        Margin="0,0,8,0"/>
                <Button x:Name="CloseButton" 
                        Content="{DynamicResource Common_Close}" 
                        Click="CloseButton_Click"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="80"
                        IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>