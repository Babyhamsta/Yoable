<Window x:Class="YoableWPF.TeamPairingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="{DynamicResource TeamPairing_Title}"
        Width="560"
        Height="480"
        MinWidth="460"
        MinHeight="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,16">
            <StackPanel>
                <TextBlock Text="{DynamicResource TeamPairing_Title}"
                           FontSize="18"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{DynamicResource TeamPairing_Subtitle}"
                           Margin="0,6,0,0"
                           FontSize="12"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20,14,20,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="28"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="{DynamicResource TeamPairing_Body}"
                       FontSize="11"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="2"
                       Text="{DynamicResource TeamPairing_Head}"
                       FontSize="11"
                       FontWeight="SemiBold"/>
        </Grid>

        <ScrollViewer Grid.Row="2" Margin="20,0" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="PairList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="28"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0"
                                      ItemsSource="{Binding Classes}"
                                      SelectedItem="{Binding SelectedBody, Mode=TwoWay}"
                                      DisplayMemberPath="Name"/>
                            <TextBlock Grid.Column="1"
                                       Text="↔"
                                       FontSize="16"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="2"
                                      ItemsSource="{Binding Classes}"
                                      SelectedItem="{Binding SelectedHead, Mode=TwoWay}"
                                      DisplayMemberPath="Name"/>
                            <Button Grid.Column="3"
                                    Content="✕"
                                    Width="28"
                                    Height="28"
                                    Margin="8,0,0,0"
                                    Click="RemovePair_Click"
                                    Tag="{Binding}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="20,14,20,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="{DynamicResource TeamPairing_AddPair}"
                    HorizontalAlignment="Left"
                    Padding="14,6"
                    Click="AddPair_Click"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="{DynamicResource Common_Cancel}"
                        Width="96"
                        Margin="0,0,8,0"
                        IsCancel="True"
                        Click="Cancel_Click"/>
                <Button Content="{DynamicResource Common_OK}"
                        Width="96"
                        IsDefault="True"
                        Click="Confirm_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
