<Window x:Class="YoableWPF.ClassConfidenceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="{DynamicResource AIClassConfidence_Title}"
        Width="640"
        Height="560"
        MinWidth="540"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,16">
            <StackPanel>
                <TextBlock Text="{DynamicResource AIClassConfidence_Title}"
                           FontSize="18"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{DynamicResource AIClassConfidence_Subtitle}"
                           Margin="0,6,0,0"
                           FontSize="12"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20,14,20,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="28"/>
                <ColumnDefinition Width="2*" MinWidth="130"/>
                <ColumnDefinition Width="3*" MinWidth="180"/>
                <ColumnDefinition Width="90"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1"
                       Text="{DynamicResource AIClassConfidence_Class}"
                       FontSize="11"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="2"
                       Text="{DynamicResource AIClassConfidence_Threshold}"
                       FontSize="11"
                       FontWeight="SemiBold"/>
        </Grid>

        <ScrollViewer Grid.Row="2"
                      Margin="20,0"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ThresholdItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                                BorderThickness="0,0,0,1"
                                Padding="0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="28"/>
                                    <ColumnDefinition Width="2*" MinWidth="130"/>
                                    <ColumnDefinition Width="3*" MinWidth="180"/>
                                    <ColumnDefinition Width="74"/>
                                    <ColumnDefinition Width="16"/>
                                </Grid.ColumnDefinitions>
                                <Rectangle Grid.Column="0"
                                           Width="12"
                                           Height="12"
                                           RadiusX="2"
                                           RadiusY="2"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Center"
                                           Fill="{Binding ColorBrush}"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding ClassId, StringFormat=ID: {0}}"
                                               FontSize="10"
                                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                                </StackPanel>
                                <Slider Grid.Column="2"
                                        Height="28"
                                        Margin="8,0,12,0"
                                        Minimum="0"
                                        Maximum="100"
                                        TickFrequency="5"
                                        IsSnapToTickEnabled="False"
                                        Value="{Binding ThresholdPercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <xctk:DoubleUpDown Grid.Column="3"
                                                   Height="28"
                                                   Minimum="0"
                                                   Maximum="100"
                                                   Increment="1"
                                                   FormatString="F0"
                                                   Value="{Binding ThresholdPercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Column="4"
                                           Text="%"
                                           Margin="4,0,0,0"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="20,14,20,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="1" Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="{DynamicResource AIClassConfidence_Reset}"
                        Width="210"
                        HorizontalAlignment="Left"
                        Click="Reset_Click"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="{DynamicResource Common_Cancel}"
                            Width="96"
                            Margin="0,0,8,0"
                            IsCancel="True"
                            Click="Cancel_Click"/>
                    <Button Content="{DynamicResource Common_OK}"
                            Width="96"
                            IsDefault="True"
                            Click="Confirm_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
